[package]
name = "audit-filter"
version = "0.1.0"
edition = "2021"

[dependencies]
hyper = { version = "0.14", features = ["full"] }
tokio = { version = "1", features = ["full"] }
# 移除 http 和 uuid
futures = "0.3"

[dev-dependencies]
tokio-test = "0.4"